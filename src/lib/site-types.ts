/**
 * Re-export stub for backward compatibility.
 * All Site types are now defined in types.ts.
 */
export type { SiteConstraint, SiteDefault, SiteType } from "./types";
export { SiteTypeSchema } from "./types";
