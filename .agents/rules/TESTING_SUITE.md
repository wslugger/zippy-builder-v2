# Testing Suite Standards

- Use Jest and React Testing Library.
- Always write a failing test first for new bugs or features.
- Tests should be located in `__tests__/`.
- Use descriptive test names.
- Mock external dependencies (like Firebase) where appropriate.
